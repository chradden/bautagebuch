services:
  app:
    build: .
    container_name: instandhaltung
    restart: unless-stopped
    ports:
      - "8090:8090"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:///data/bautagebuch.db
      - PDF_OUTPUT_DIR=./data/output
      - UPLOAD_DIR=./data/uploads
